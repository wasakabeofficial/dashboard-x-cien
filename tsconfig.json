{
  "extends": "@vue/tsconfig/tsconfig.dom.json",
  "include": ["env.d.ts", "src/**/*", "src/**/*.vue", "vite.config.*"],
  "exclude": ["src/**/__tests__/*"],
  "compilerOptions": {
    // Silence deprecation warnings for TS < 7 options (baseUrl, etc.)
    "ignoreDeprecations": "6.0",

    // Base URL required for paths alias resolution in VS Code
    "baseUrl": ".",

    // Extra safety for array and object lookups, but may have false positives.
    "noUncheckedIndexedAccess": true,

    // Explicit types for .vue SFC resolution in VS Code
    "types": ["vite/client"],

    // Path mapping for cleaner imports.
    "paths": {
      "@/*": ["./src/*"]
    },

    // `vue-tsc --build` produces a .tsbuildinfo file for incremental type-checking.
    // Specified here to keep it out of the root directory.
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo"
  }
}
